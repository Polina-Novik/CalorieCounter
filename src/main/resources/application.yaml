spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_db1?useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
      sql:
        init:
          mode: always
          continue-on-error: true

jwt:
  secret: security_calorie_counter





